package com.example.mall.product.product_image;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductImageJPARepository extends JpaRepository<ProductImage, Integer> {
}
